const $=s=>document.querySelector(s), read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}}, write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
let saved=read('rafProfile',null),clients=read('rafClients',[]),programs=read('rafPrograms',{});
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function normalize(p){p=p&&typeof p==='object'?p:{};p.variables=p.variables||{};p.exercises=Array.isArray(p.exercises)?p.exercises:[];p.exercises=p.exercises.map(e=>({...e,muscles:Array.isArray(e.muscles)?e.muscles:[],equipment:e.equipment||'Not specified',instruction:e.instruction||'Controlled technique.'}));return p}
function base(){return normalize(window.RAF?.buildProgram?.(saved)||{})} function current(){let b=base(),s=programs[saved?.name];return normalize({...b,...(s||{}),variables:{...b.variables,...(s?.variables||{})},exercises:Array.isArray(s?.exercises)?s.exercises:b.exercises})}
function opts(id){return (window.RAF?.RAF_EXERCISES||[]).map(e=>`<option value="${esc(e.id)}" ${e.id===id?'selected':''}>${esc(e.name)}</option>`).join('')}
const views={dashboard:['Dashboard',()=>`<div class="hero"><p class="eyebrow">RAF COACHING</p><h2>Build a smarter coaching journey.</h2><p class="muted">Manage clients, assessments and individualized plans.</p><button class="primary" data-view="clients">Open clients →</button></div><div class="grid"><div class="card"><div class="stat">${clients.length}</div><div class="label">Clients</div></div></div>`],clients:['Clients',()=>`<div class="hero"><h2>Your clients</h2><button class="primary" data-view="assessment">+ Add client</button></div><div class="card">${clients.length?clients.map((c,i)=>`<div class="workout"><div><strong>${esc(c.name)}</strong><div class="muted">Age ${esc(c.age)} · ${esc(c.goal)} · ${esc(c.days)} days/week</div></div><button class="secondary" data-client="${i}">Open profile</button></div>`).join(''):'<p class="muted">No clients yet.</p>'}</div>`],assessment:['Client Assessment',()=>`<div class="hero"><h2>Create a client profile.</h2></div><form id="assessment-form" class="card form-grid"><label>Name<input name="name" required value="${esc(saved?.name)}"></label><label>Age<input name="age" type="number" min="13" max="100" required value="${esc(saved?.age)}"></label><label>Goal<select name="goal"><option value="muscle">Muscle gain</option><option value="fat-loss">Fat loss</option><option value="strength">Strength</option></select></label><label>Experience<select name="experience"><option>beginner</option><option>intermediate</option><option>advanced</option></select></label><label>Training days<input name="days" type="number" min="1" max="7" value="${saved?.days||4}"></label><label>Equipment<select name="equipment"><option value="gym">Full gym</option><option value="home">Home equipment</option></select></label><button class="primary" type="submit">Save client & generate program</button></form>`],program:['Program Builder',()=>programHTML()],rules:['NASM Rules',()=>`<div class="hero"><h2>NASM / OPT rules</h2><p class="muted">Decision support only. The coach approves every program.</p></div>`],workout:['Workout',()=>`<div class="card"><h2>Workout</h2><p class="muted">Open a client profile to view and customize the workout.</p></div>`],nutrition:['Nutrition',()=>`<div class="card"><h2>Nutrition</h2><p class="muted">Nutrition workspace is ready for the next module.</p></div>`],progress:['Progress',()=>`<div class="card"><h2>Progress</h2><p class="muted">Progress tracking workspace is ready for the next module.</p></div>`]};
function programHTML(){if(!saved)return`<div class="card"><p class="muted">Complete the assessment first.</p><button class="primary" data-view="assessment">Open assessment</button></div>`;let p=current(),v=p.variables;return`<div class="hero"><h2>${esc(saved.name)} · Phase ${esc(p.phase||'')}</h2><p class="muted">${esc(p.phaseName||v.name||'')} · ${esc(p.goal||saved.goal||'')}</p></div><form id="program-form"><div class="card form-grid"><label>Sets<input name="sets" type="number" min="1" max="10" value="${v.sets||3}"></label><label>Reps<input name="reps" value="${esc(v.reps)}"></label><label>Tempo<input name="tempo" value="${esc(v.tempo)}"></label><label>Rest<input name="rest" value="${esc(v.rest)}"></label><label>RIR<input name="rir" type="number" min="0" max="5" value="${v.rir??2}"></label></div><div class="card">${p.exercises.map((e,i)=>`<div class="workout"><div><strong>${i+1}. ${esc(e.name)}</strong><div class="muted">${esc(e.muscles.join(' · '))} · ${esc(e.equipment)}</div><small>${esc(e.instruction)}</small><label>Replace exercise<select name="exercise_${i}">${opts(e.id)}</select></label></div></div>`).join('')}</div><button class="primary" type="submit">Save customized program</button><p id="program-status" class="muted"></p></form>`}
function bind(){document.querySelectorAll('[data-view]').forEach(x=>x.onclick=()=>{location.hash=x.dataset.view;render(x.dataset.view)});document.querySelectorAll('[data-client]').forEach(x=>x.onclick=()=>{saved=clients[+x.dataset.client];write('rafProfile',saved);location.hash='program';render('program')});let f=$('#assessment-form');if(f)f.onsubmit=e=>{e.preventDefault();let d=Object.fromEntries(new FormData(f));d.age=+d.age;d.days=+d.days;saved=d;clients=[...clients.filter(c=>c.name!==d.name),d];write('rafProfile',d);write('rafClients',clients);if(!programs[d.name])programs[d.name]=normalize(window.RAF?.buildProgram?.(d)||{});write('rafPrograms',programs);location.hash='program';render('program')};let p=$('#program-form');if(p)p.onsubmit=e=>{e.preventDefault();let q=current(),d=Object.fromEntries(new FormData(p));q.variables={...q.variables,sets:+d.sets,reps:d.reps,tempo:d.tempo,rest:d.rest,rir:+d.rir};q.exercises=q.exercises.map((old,i)=>normalize({exercises:[(window.RAF?.RAF_EXERCISES||[]).find(x=>x.id===d[`exercise_${i}`])||old]}).exercises[0]);programs[saved.name]=q;write('rafPrograms',programs);$('#program-status').textContent='Saved locally ✓'}}
function render(name){saved=read('rafProfile',null);clients=read('rafClients',[]);programs=read('rafPrograms',{});let v=views[name]||views.dashboard;$('#page-title').textContent=v[0];$('#app-content').innerHTML=v[1]();document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view===name));bind()}render(location.hash.slice(1)||'dashboard');