const content=document.querySelector('#app-content');const title=document.querySelector('#page-title');let saved=JSON.parse(localStorage.getItem('rafProfile')||'null');let clients=JSON.parse(localStorage.getItem('rafClients')||'[]');
const views={dashboard:{title:'Dashboard',html:()=>`<div class="hero"><p class="eyebrow">RAF COACHING</p><h2>Build a smarter coaching journey.</h2><p class="muted">Manage clients, assessments and individualized NASM/OPT-based plans.</p><button class="primary" data-view="clients">Open clients →</button></div><div class="grid"><div class="card"><div class="stat">${clients.length}</div><div class="label">Clients</div></div><div class="card"><div class="stat">${saved?RAF.selectOPTPhase(saved):'—'}</div><div class="label">Current OPT phase</div></div></div>`},clients:{title:'Clients',html:()=>`<div class="hero"><p class="eyebrow">COACH WORKSPACE</p><h2>Your clients</h2><p class="muted">Create a client profile and open their coaching workspace.</p><button class="primary" data-view="assessment">+ Add client</button></div><div class="card">${clients.length?clients.map((c,i)=>`<div class="workout"><div><strong>${c.name}</strong><div class="muted">Age ${c.age} · ${c.goal} · ${c.days} days/week</div></div><button class="secondary" data-client="${i}">Open profile</button></div>`).join(''):'<p class="muted">No clients yet. Add your first client.</p>'}</div>`},assessment:{title:'Client Assessment',html:()=>`<div class="hero"><p class="eyebrow">STEP 1 · ASSESSMENT</p><h2>Create a client profile.</h2><p class="muted">The coach remains the final decision-maker. This tool does not diagnose injuries.</p></div><form id="assessment-form" class="card form-grid"><label>Name<input name="name" required value="${saved?.name||''}"></label><label>Age<input name="age" type="number" min="13" max="100" required value="${saved?.age||''}"></label><label>Goal<select name="goal"><option value="muscle">Muscle gain</option><option value="fat-loss">Fat loss</option><option value="strength">Strength</option><option value="power">Power</option></select></label><label>Experience<select name="experience"><option value="beginner">Beginner</option><option value="intermediate">Intermediate</option><option value="advanced">Advanced</option></select></label><label>Technique<select name="technique"><option value="good">Good technique</option><option value="needs-regression">Needs regression</option></select></label><label>Limitations<select name="limitations"><option value="">None reported</option><option value="knee">Knee limitation</option><option value="back">Back limitation</option></select></label><label>Training days<input name="days" type="number" min="1" max="7" value="4"></label><label>Equipment<select name="equipment"><option value="gym">Full gym</option><option value="home">Home equipment</option></select></label><button class="primary" type="submit">Save client & generate program</button></form>`},program:{title:'Generated Program',html:()=>programHTML()}};
function programHTML(){if(!saved)return '<div class="card"><p class="muted">Complete the assessment first.</p><button class="primary" data-view="assessment">Open assessment</button></div>';const p=RAF.buildProgram(saved);return `<div class="hero"><p class="eyebrow">NASM / OPT OUTPUT</p><h2>${saved.name} · Phase ${p.phase}</h2><p class="muted">${p.variables.sets} sets · ${p.variables.reps} reps · Tempo ${p.variables.tempo} · Rest ${p.variables.rest} · RIR ${p.variables.rir}</p></div><div class="card">${p.exercises.map((e,i)=>`<div class="workout"><div><strong>${i+1}. ${e.name}</strong><div class="muted">${e.muscles.join(' · ')} · ${e.equipment}</div></div><span class="tag">${p.variables.reps}</span></div>`).join('')}</div>`}
function render(view='dashboard'){const v=views[view]||views.dashboard;title.textContent=v.title;content.innerHTML=typeof v.html==='function'?v.html():v.html;document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view===view));document.querySelectorAll('[data-view]').forEach(x=>x.onclick=()=>render(x.dataset.view));document.querySelectorAll('[data-client]').forEach(x=>x.onclick=()=>{saved=clients[Number(x.dataset.client)];localStorage.setItem('rafProfile',JSON.stringify(saved));render('program')});const form=document.querySelector('#assessment-form');if(form)form.onsubmit=e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));data.age=Number(data.age);data.limitations=Boolean(data.limitations);saved=data;clients=[...clients.filter(c=>c.name!==data.name),data];localStorage.setItem('rafProfile',JSON.stringify(data));localStorage.setItem('rafClients',JSON.stringify(clients));location.hash='program';render('program')}}render(location.hash.replace('#','')||'dashboard');