/* RAF Coaching — client assessment photos + hide legacy review button */
(function(){'use strict';
 const read=(k,f=null)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}};
 const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
 function hideReview(){document.getElementById('raf-review-button')?.remove();document.querySelectorAll('#raf-review-button').forEach(x=>x.remove())}
 function addPhotos(){const form=document.getElementById('client-assessment-form');if(!form||form.querySelector('#raf-photo-fields'))return;const box=document.createElement('section');box.id='raf-photo-fields';box.className='assessment-panel';box.innerHTML='<h3>Assessment photos</h3><p class="muted">Upload front, side, and back photos. These will be attached to your client file.</p><div class="form-grid"><label>Front photo<input type="file" accept="image/*" name="front_photo"></label><label>Side photo<input type="file" accept="image/*" name="side_photo"></label><label>Back photo<input type="file" accept="image/*" name="back_photo"></label></div>';const actions=form.querySelector('.assessment-actions');form.insertBefore(box,actions);}
 function dataURL(file){return new Promise((resolve,reject)=>{if(!file)return resolve('');const r=new FileReader();r.onload=()=>resolve(r.result);r.onerror=reject;r.readAsDataURL(file)})}
 async function savePhotos(){const form=document.getElementById('client-assessment-form');if(!form)return;const files=['front_photo','side_photo','back_photo'];const photos={};for(const n of files){const f=form.elements[n]?.files?.[0];if(f)photos[n]=await dataURL(f)}if(!Object.keys(photos).length)return;const s=read('rafSession',{}),db=window.rafSupabase;if(db&&s.id){setTimeout(async()=>{const q=await db.from('client_onboarding').select('id,notes').eq('client_id',s.id).maybeSingle();if(q.error||!q.data)return;let n={};try{n=typeof q.data.notes==='string'?JSON.parse(q.data.notes):(q.data.notes||{})}catch{};Object.assign(n,photos);await db.from('client_onboarding').update({notes:JSON.stringify(n)}).eq('id',q.data.id)},1200)}const c=read('rafClientOnboarding',{});Object.assign(c,photos);localStorage.setItem('rafClientOnboarding',JSON.stringify(c))}
 const obs=new MutationObserver(()=>{hideReview();addPhotos()});obs.observe(document.body,{childList:true,subtree:true});document.addEventListener('submit',e=>{if(e.target?.id==='client-assessment-form')savePhotos()},true);document.addEventListener('DOMContentLoaded',()=>{hideReview();addPhotos()});window.addEventListener('raf-auth-ready',hideReview);
})();