(function(){'use strict';function read(k,f){try{return JSON.parse(localStorage.getItem(k))||f}catch(e){return f}}function fix(){var s=read('rafSession',{});if(s.role==='coach')localStorage.removeItem('rafProfile');var c=s.role==='coach'?read('rafActiveClient',null):read('rafProfile',null);if(location.hash!=='#nutrition'||!s.role)return;if(s.role==='coach'&&!c){var r=document.querySelector('#app-content');if(r)r.innerHTML='<section class="hero"><p class="eyebrow">COACH WORKSPACE</p><h2>Select a client first</h2><p class="muted">افتح ملف العميل من Clients ثم اضغط Nutrition. لا تكتب اسمك ولا تنشئ خطة لنفسك من حساب الكوتش.</p><button class="primary" id="ctx-back">Back to Clients</button></section>';document.querySelector('#ctx-back')?.addEventListener('click',function(){window.render&&window.render('clients')})}}document.addEventListener('DOMContentLoaded',function(){setTimeout(fix,200)});window.addEventListener('hashchange',function(){setTimeout(fix,100)});})();