/* RAF Nutrition — unlimited food items per meal, live totals */
(function(){'use strict';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch(e){return f}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
const key=c=>c?.id||c?.email||c?.name||'default';
function foods(){return window.RAF_FOODS||[]}
function calculate(form){const totals=[];form.querySelectorAll('fieldset.meal-box').forEach(box=>{const x={calories:0,protein:0,carbs:0,fat:0,items:[]};box.querySelectorAll('select[name^="food_"]').forEach(sel=>{const suffix=sel.name.slice(5),grams=+form.querySelector(`[name="grams_${suffix}"]`)?.value||0,i=sel.value,f=foods()[+i];if(i!==''&&grams>0&&f){const q=grams/100;x.calories+=f[2]*q;x.protein+=f[3]*q;x.carbs+=f[4]*q;x.fat+=f[5]*q;x.items.push({name:f[0],grams})}});x.calories=Math.round(x.calories);x.protein=Math.round(x.protein);x.carbs=Math.round(x.carbs);x.fat=Math.round(x.fat);totals.push(x)});return totals}
function addRows(){const form=document.querySelector('#meal-builder');if(!form||form.dataset.multiFood==='1')return;form.dataset.multiFood='1';form.querySelectorAll('fieldset.meal-box').forEach((box,m)=>{for(let r=5;r<=8;r++){const row=document.createElement('div');row.className='form-grid meal-row';row.innerHTML=`<select name="food_${m}_${r}"><option value="">Choose another food</option></select><input name="grams_${m}_${r}" type="number" min="0" placeholder="grams">`;const source=box.querySelector('select[name^="food_"]');if(source)row.querySelector('select').innerHTML=source.innerHTML.replace('Choose food','Choose another food');box.appendChild(row)}const hint=document.createElement('p');hint.className='muted';hint.textContent='You can add several foods in the same meal.';box.appendChild(hint)});const refresh=()=>{const totals=calculate(form);let live=document.querySelector('#raf-live-meal-totals');if(!live){live=document.createElement('section');live.id='raf-live-meal-totals';live.className='card';form.parentNode.appendChild(live)}live.innerHTML=`<p class="eyebrow">LIVE TOTALS</p>${totals.map((x,i)=>`<p><b>Meal ${i+1}:</b> ${x.calories} kcal · P ${x.protein}g · C ${x.carbs}g · F ${x.fat}g</p>`).join('')}<p><b>Day total:</b> ${totals.reduce((a,x)=>a+x.calories,0)} kcal · P ${totals.reduce((a,x)=>a+x.protein,0)}g · C ${totals.reduce((a,x)=>a+x.carbs,0)}g · F ${totals.reduce((a,x)=>a+x.fat,0)}g</p>`};form.addEventListener('input',refresh);form.addEventListener('change',refresh);form.addEventListener('submit',e=>{e.preventDefault();e.stopImmediatePropagation();const all=read('rafNutritionPlans',{}),c=read('rafActiveClient',null)||read('rafProfile',null)||{name:'Client'},k=key(c),plan=all[k]||{};plan.mealTotals=calculate(form);all[k]=plan;write('rafNutritionPlans',all);window.render?.('nutrition')},true);refresh()}
new MutationObserver(addRows).observe(document.body,{childList:true,subtree:true});addRows();
})();