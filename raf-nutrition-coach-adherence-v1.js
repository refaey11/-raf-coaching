(()=>{'use strict';
const U='https://zkymvovbpfrwjyfwylbq.supabase.co',K='sb_publishable_9SgCU4D-48kgotUdi79gfQ_NaouEXbO';const db=()=>window.supabase?.createClient?window.supabase.createClient(U,K,{auth:{persistSession:true,autoRefreshToken:true,storageKey:'raf-auth-v3'}}):null;const role=()=>window.RAF_AUTH_ROLE||{};const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function render(){if(!role().isCoach)return;const root=document.getElementById('app-content');if(!root||!document.querySelector('[data-view="nutrition"].active')||document.getElementById('raf-coach-nutrition-adherence'))return;const box=document.createElement('section');box.id='raf-coach-nutrition-adherence';box.className='card';box.innerHTML='<div class="eyebrow">NUTRITION ADHERENCE</div><h2>Client nutrition progress</h2><p class="muted">Loading adherence data…</p>';root.appendChild(box);const s=db(),coach=role().userId;if(!s||!coach)return;const links=await s.from('coach_clients').select('client_id').eq('coach_id',coach);if(links.error)return;const ids=(links.data||[]).map(x=>x.client_id);if(!ids.length){box.innerHTML='<div class="eyebrow">NUTRITION ADHERENCE</div><h2>No clients assigned</h2>';return}const [pr,lr]=await Promise.all([s.from('profiles').select('id,full_name,email').in('id',ids),s.from('raf_nutrition_adherence_daily').select('*').in('client_id',ids).order('log_date',{ascending:false}).limit(300)]);if(lr.error){box.innerHTML='<h2>Nutrition progress</h2><p class="muted">Could not load adherence logs.</p>';return}const by={};(lr.data||[]).forEach(x=>{if(!by[x.client_id])by[x.client_id]=x});box.innerHTML='<div class="raf-adherence-grid">'+(pr.data||[]).map(c=>{const x=by[c.id],pct=x&&x.meals_planned?Math.min(100,Math.round(100*x.meals_completed/x.meals_planned)):0,cal=x?Math.round(Number(x.calories_consumed||0)):0,target=x?Math.round(Number(x.calories_target||0)):0,pp=x?Math.round(Number(x.protein_consumed_g||0)):0,pt=x?Math.round(Number(x.protein_target_g||0)):0;return '<article class="raf-adherence-client"><div><strong>'+esc(c.full_name||c.email||'Client')+'</strong><span class="raf-adherence-pill">'+(x?esc(x.log_date):'No log')+'</span></div><div class="raf-adherence-bar"><i style="width:'+pct+'%"></i></div><b>'+pct+'% meal adherence</b><small>'+(x?esc(x.meals_completed)+' / '+esc(x.meals_planned)+' meals</small><div class="raf-adherence-macros"><span>'+cal+' / '+target+' kcal</span><span>'+pp+' / '+pt+'g P</span><span>'+Math.round(Number(x.carbs_consumed_g||0))+' / '+Math.round(Number(x.carbs_target_g||0))+'g C</span><span>'+Math.round(Number(x.fat_consumed_g||0))+' / '+Math.round(Number(x.fat_target_g||0))+'g F</span></div>':'No nutrition activity recorded')+'</article>'}).join('')+'</div>';}
new MutationObserver(render).observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['class']});document.addEventListener('DOMContentLoaded',render);window.RAF_NUTRITION_COACH_ADHERENCE={render};})();