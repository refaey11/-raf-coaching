/* RAF Coaching — Nutrition Engine v2 */
(function(){
 const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}},write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 function nutrition(){const p=read('rafProfile',null);if(!p)return '<div class="card"><h2>Nutrition</h2><p class="muted">Complete the assessment first.</p></div>';const all=read('rafNutrition',{}),n=all[p.name]||{},cal=+n.calories||2000,protein=+n.protein||150,carbs=+n.carbs||200,fat=+n.fat||65,meals=+n.meals||4;return `<div class="hero"><h2>${esc(p.name)} · Nutrition</h2><p class="muted">Coach-reviewed nutrition planning workspace.</p></div><form id="raf-nutrition-form" class="card form-grid"><label>Calories<input name="calories" type="number" min="800" max="10000" value="${cal}"></label><label>Protein (g)<input name="protein" type="number" min="0" value="${protein}"></label><label>Carbs (g)<input name="carbs" type="number" min="0" value="${carbs}"></label><label>Fat (g)<input name="fat" type="number" min="0" value="${fat}"></label><label>Meals/day<input name="meals" type="number" min="1" max="8" value="${meals}"></label><label>Notes<textarea name="notes">${esc(n.notes||'')}</textarea></label><div class="card"><strong>Macro check</strong><p class="muted">Protein: ${protein*4} kcal · Carbs: ${carbs*4} kcal · Fat: ${fat*9} kcal</p><p><strong>Total from macros: ${protein*4+carbs*4+fat*9} kcal</strong></p><p class="muted">Approximate daily meal targets: ${Math.round(cal/meals)} kcal per meal.</p></div><button class="primary" type="submit">Save nutrition plan</button><p id="raf-nutrition-status" class="muted"></p></form>`}
 const old=window.render;window.render=function(name){if(name!=='nutrition')return old(name);document.querySelector('#page-title').textContent='Nutrition';document.querySelector('#app-content').innerHTML=nutrition();document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.view===name));const f=document.querySelector('#raf-nutrition-form');if(f)f.onsubmit=e=>{e.preventDefault();const p=read('rafProfile',null),d=Object.fromEntries(new FormData(f));['calories','protein','carbs','fat','meals'].forEach(k=>d[k]=+d[k]);const all=read('rafNutrition',{});all[p.name]=d;write('rafNutrition',all);document.querySelector('#raf-nutrition-status').textContent='Saved locally ✓';window.render('nutrition')}};
})();