(()=>{
  const foods={Eggs:[70,6,0,5],'Chicken breast':[165,31,0,4],Tuna:[116,26,0,1],'Domty white cheese':[250,14,4,20],'Rice cooked':[130,3,28,0],Oats:[389,17,66,7],Potato:[87,2,20,0],Banana:[89,1,23,0],Apple:[52,0,14,0],'Olive oil':[884,0,0,100],Almonds:[579,21,22,50],Vegetables:[30,2,6,0]};
  const q=s=>document.querySelector(s);
  function key(){const c=(()=>{try{return JSON.parse(localStorage.getItem('rafActiveClient'))||JSON.parse(localStorage.getItem('rafProfile'))}catch{return null}})();return c?.id||c?.email||c?.name||'default'}
  function plan(){try{return JSON.parse(localStorage.getItem('rafNutritionPlans'))?.[key()]||{}}catch{return {}}}
  function totals(){let kcal=0,p=0,c=0,f=0;document.querySelectorAll('[data-meal]').forEach((sel,i)=>{const g=+(document.querySelector(`[data-grams="${i}"]`)?.value||0);const v=foods[sel.value];if(v){kcal+=v[0]*g/100;p+=v[1]*g/100;c+=v[2]*g/100;f+=v[3]*g/100}});return {kcal:Math.round(kcal),p:Math.round(p),c:Math.round(c),f:Math.round(f)}}
  function paint(){const box=q('#raf-remaining-targets');if(!box)return;const n=plan(),t=totals();const item=(label,remain,used,unit)=>`<div><span>${label} remaining</span><strong>${Math.max(0,Math.round(remain))} <small>${unit}</small></strong><small>Used ${used} / ${n[label==='Calories'?'calories':label==='Protein'?'protein':label==='Carbs'?'carbs':'fat']||0} ${unit}</small></div>`;box.innerHTML=`<p class="eyebrow">REMAINING TARGETS</p><div class="raf-remaining-grid">${item('Calories',(+n.calories||0)-t.kcal,t.kcal,'kcal')}${item('Protein',(+n.protein||0)-t.p,t.p,'g')}${item('Carbs',(+n.carbs||0)-t.c,t.c,'g')}${item('Fat',(+n.fat||0)-t.f,t.f,'g')}</div>`}
  function enhance(){if(location.hash.slice(1)!=='nutrition')return;const card=[...document.querySelectorAll('.card')].find(x=>x.querySelector('#save-meals'));if(!card)return;let box=q('#raf-remaining-targets');if(!box){box=document.createElement('div');box.id='raf-remaining-targets';box.className='card raf-remaining-targets';card.after(box)}document.querySelectorAll('[data-meal],[data-grams]').forEach(x=>{x.oninput=paint;x.onchange=paint});paint()}
  const old=window.render;window.render=function(v){old(v);setTimeout(enhance,0)};window.addEventListener('hashchange',()=>setTimeout(enhance,0));setInterval(()=>{if(location.hash.slice(1)==='nutrition')enhance()},700);setTimeout(enhance,0);
})();